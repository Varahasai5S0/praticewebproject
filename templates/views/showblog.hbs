<!DOCTYPE html>
<html>
    <head>
        {{>link}}
    </head>
    <body>
        {{>header}}
        <div class="container-fluid px-3 py-4">
            <a class="text-danger" href="/blog">All articles/</a>
            <div class="card">
                <h1>{{post.title}}</h1>
                <h2>{{post.description}}</h2>
                <h3>{{{post.sanitizedHtml}}}</h3>
            </div>
        </div>
        <h1>Comments::</h1>
        <div class="container">
            {{#each post.comments}}
                <div class="card">
                    <h1>{{this.comment}}</h1>
                </div>
            {{/each}}
        </div>
        <div class="form-group">
            <form action='/blog/check/{{post.slug}}' method='POST'>
    <label for="exampleFormControlTextarea1">Give a comment</label>
    <textarea class="form-control" name="comm" id="textarea" required></textarea>
  <button type="submit" class="btn btn-primary">Add</button>
  </form>
  </div>
        
    </body>
</html>